node_modules/
dist/
web/tmp/
server/data/
bin/yt-dlp.exe
bin/ffmpeg.exe
bin/*.exe
# yt-dlp's onedir build: the exe is a launcher and _internal holds the
# ~24MB of Python it needs beside it. Downloaded, not committed.
bin/_internal/

# Rust build output for the native engine. The source is committed; the
# ~2MB DLL is built with `npm run engine:build`, or dropped into bin/ by CI.
engine/target/
bin/velox_engine.dll

.DS_Store
.claude/

# Secrets — never commit
.env
server/.env
*.env
!*.env.example

# Server details for npm run release — local only
release.config.json

# Rust build output for the licence server port
server-rs/target/
