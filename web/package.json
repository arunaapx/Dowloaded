{
  "name": "velox-web",
  "version": "0.1.0",
  "private": true,
  "description": "Velox Downloader web app (PWA frontend + server-side yt-dlp API).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
